version: "3.9"

services:
  mm_bot_dev:
    build: .
    container_name: mm_bot_dev
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./config.json:/app/config.json:ro
      - ./data.xlsx:/app/data.xlsx
